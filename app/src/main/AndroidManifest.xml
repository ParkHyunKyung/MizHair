<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cookandroid.charm_admin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CustomerManagement.ContactsListActivity"/>
        <activity android:name=".CustomerManagement.CustomerActivity"/>
        <activity android:name=".Reservation.ReservationActivity"/>
        <activity android:name=".Notice.NoticeActivity"/>
        <activity android:name=".Notice.NoticeListActivity" android:label="NoticeActivity"/>
        <activity android:name=".PriceList.PriceListActivity" android:label="PriceListActivity"/>
        <activity android:name=".ReservationList.ReservationListActivity"/>
    </application>

</manifest>